include "application.conf"

play.server.pidfile.path=/home/catis/RUNNING_PID_KI
#play.server.pidfile.path=/var/run/catis/RUNNING_PID

play.http.context="/ki/catis"
http.port=9007

play.filters.hosts {
  allowed = [".upmind.ee", "localhost:9000"]
}

mongodb.uri ="mongodb://localhost:27017/catis_ki"
mongodb {
  db="catis_ki"
  servers=["localhost:27017"]
}

# play.crypto.secret=${?PLAY_APP_SECRET}
play.http.secret.key=pleasechangemetoo.ki

silhouette {
  # Authenticator settings
  authenticator.cookieDomain="upmind.ee"
  authenticator.secureCookie=true
  authenticator.cookiePath="/ki/"
}

registration.sendto = "catis@upmind.ee"

play.mailer {
  host = mail.upmind.ee
  from = "CatIS <catis@upmind.ee>"
  #  port (defaults to 25)
  #  ssl (defaults to no)
  #  tls (defaults to no)
  #  user (optional)
  #  password (optional)
  #  debug (defaults to no, to take effect you also need to set the log level to "DEBUG" for the application logger)
  #  timeout (defaults to 60s in milliseconds)
  #  connectiontimeout (defaults to 60s in milliseconds)
  #  mock (defaults to no, will only log all the email properties instead of sending an email)
}